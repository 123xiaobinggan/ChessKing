import 'package:get/get.dart';
import 'package:flutter/material.dart';

class ChineseChessRank extends StatelessWidget {
  const ChineseChessRank({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('中国象棋排行榜')));
  }
}
