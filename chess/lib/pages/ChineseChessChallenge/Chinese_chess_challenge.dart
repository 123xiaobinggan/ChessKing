import 'package:get/get.dart';
import 'package:flutter/material.dart';

class ChineseChessChallenge extends StatelessWidget {
  const ChineseChessChallenge({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('中国象棋挑战')));
  }
}
